#include "logscan/LogScanner.h"

namespace logscan {

// The orchestration implementation is intentionally left empty. Add the
// source lifecycle, bounded queue, worker lifecycle, cancellation, ordering,
// error propagation, and report aggregation here.
class LogScanner::Impl {};

}  // namespace logscan
